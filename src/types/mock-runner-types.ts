import {
    MockPlaygroundConfigSchema,
    MockPlaygroundConfigType,
} from '@ondc/automation-mock-runner';

export type MockRunnerConfig = MockPlaygroundConfigType;
export const MockRunnerConfigSchema = MockPlaygroundConfigSchema;
